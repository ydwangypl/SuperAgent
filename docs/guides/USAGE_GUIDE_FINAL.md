# SuperAgent v3.1 使用指南 (最终版)

**重要更新: 简称冲突说明**

---

## ⚠️ 重要: "SA" 简称已被占用

### 冲突说明

您的系统中已经安装了 **Serena** 工具 (一个代码搜索和分析的 MCP 工具),它已经占用了 "SA" 这个简称。

| 简称 | 工具名称 | 用途 |
|------|---------|------|
| **SA** | Serena | 代码搜索和分析工具 (MCP) |
| **SuperAgent** | SuperAgent | 任务编排和 AI 系统 |

**因此,不能使用 "SA" 作为 SuperAgent 的简称!**

---

## ✅ 正确的使用方式

### 方式 1: 使用完整名称 (最推荐)

```
使用 SuperAgent 开发用户登录功能
使用 SuperAgent 规划博客系统
使用 SuperAgent 查询记忆
```

**优点**: 清晰明确,不会混淆

---

### 方式 2: 使用 "SAgent" 简称

```
使用 SAgent 开发用户登录功能
使用 SAgent 规划博客系统
使用 SAgent 查询记忆
```

**优点**: 简洁,且不与 Serena 冲突

---

### 方式 3: 使用 "super" 简称

```
使用 super 开发用户登录功能
使用 super 规划博客系统
```

**优点**: 更短,容易记忆

---

## 🚀 快速开始

### 示例 1: 开发新功能

```
您: 使用 SuperAgent 开发一个用户登录功能

我 (Claude Code):
  # 导入 SuperAgent
  from orchestration import Orchestrator
  from planning import ProjectPlanner

  # 初始化
  orchestrator = Orchestrator(Path("."))
  planner = ProjectPlanner()

  # 生成计划
  plan = await planner.create_plan("开发用户登录功能")

  # 执行计划
  result = await orchestrator.execute_plan(plan)

  # 返回需求文档并生成代码
```

---

### 示例 2: 查询记忆系统

```
您: 使用 SuperAgent 查询记忆: Token验证失败

我 (Claude Code):
  from memory import MemoryManager

  mm = MemoryManager(Path("."))
  relevant = await mm.query_relevant_memory(
      task="Token验证失败",
      agent_type="backend-dev"
  )

  # 显示历史错误和修复方案
```

---

### 示例 3: 代码审查

```
您: 使用 SuperAgent 审查 user.py

我 (Claude Code):
  from review import CodeReviewer

  reviewer = CodeReviewer()
  result = reviewer.review_code(
      task_id="review-user",
      files=["src/user.py"],
      code_content={"user.py": open("src/user.py").read()}
  )

  # 显示审查结果
```

---

## 📝 常用命令参考

### 规划和开发

| 命令 | 说明 |
|------|------|
| 使用 SuperAgent 规划... | 生成执行计划 |
| 使用 SAgent 开发... | 规划 + 生成代码 |
| 使用 super 分析... | 分析现有代码 |

### 记忆管理

| 命令 | 说明 |
|------|------|
| 使用 SuperAgent 查询记忆: ... | 查询历史经验 |
| 使用 SuperAgent 查看记忆统计 | 查看统计数据 |
| 使用 SAgent 查询: ... | 查询相关错误 |

### 代码审查

| 命令 | 说明 |
|------|------|
| 使用 SuperAgent 审查代码 | 审查整个项目 |
| 使用 super 审查 user.py | 审查指定文件 |

---

## 🎯 推荐用法

### ✅ 最佳实践

1. **明确指定**: 使用 "SuperAgent" 完整名称
2. **项目说明**: 说明要开发什么功能
3. **技术栈**: 指定使用的技术栈 (可选)

### 示例对话

```
您: 使用 SuperAgent 开发一个用户登录功能,使用 Python + FastAPI

我:
  1. 生成执行计划
  2. 返回需求文档
  3. 生成 FastAPI 代码
  4. 审查代码质量
  5. 保存到记忆系统
```

---

## 🔄 完整工作流程

### Step 1: 规划

```
您: 使用 SuperAgent 规划一个博客系统

我:
  - 生成详细执行计划
  - 显示任务分解步骤
  - 显示依赖关系
```

### Step 2: 开发

```
您: 使用 SuperAgent 开发博客系统

我:
  - 执行计划
  - Agent 返回需求文档:
    * DATABASE_SCHEMA.md
    * API_SPEC.md
    * REQUIREMENTS.md
  - 根据需求生成实际代码
  - 自动审查代码质量
```

### Step 3: 学习

```
(自动执行)
  - 保存执行历史到情节记忆
  - 提取知识到语义记忆
  - 保存最佳实践到程序记忆
```

---

## 💡 高级功能

### 1. 自定义配置

```
您: 使用 SuperAgent 开发...,配置: 最大并行任务5,启用审查

我:
  from orchestration.models import OrchestrationConfig

  config = OrchestrationConfig(
      max_parallel_tasks=5,
      enable_review=True
  )

  orchestrator = Orchestrator(Path("."), config=config)
```

### 2. 查看详细步骤

```
您: 使用 SuperAgent 开发博客系统,显示详细步骤

我:
  - 显示每一步的计划
  - 显示每个 Agent 的返回
  - 显示需求文档内容
  - 显示审查报告
```

### 3. 技术栈指定

```
您: 使用 SuperAgent 开发用户登录,技术栈: Python + FastAPI + SQLAlchemy

我:
  - 根据技术栈生成需求
  - 生成符合技术栈的代码
  - 使用适合的库和框架
```

---

## 🌐 全局配置

### 设置环境变量 (推荐)

```powershell
# Windows PowerShell
[System.Environment]::SetEnvironmentVariable('SUPERAGENT_ROOT', 'E:\SuperAgent', 'User')

# 重启 PowerShell 后生效
```

### 验证配置

```bash
# 运行验证脚本
python e:\SuperAgent\setup_superagent.py
```

---

## 📊 对比表

| 特性 | Serena (SA) | SuperAgent |
|------|-------------|------------|
| **用途** | 代码搜索和分析 | 任务编排和 AI |
| **简称** | SA | SuperAgent / SAgent |
| **类型** | MCP 工具 | Python 库 |
| **功能** | 符号搜索、代码导航 | 规划、开发、审查 |
| **记忆** | 无 | 三层记忆系统 |
| **Agent** | 无 | 13 种 Agent |

---

## ❓ 常见问题

### Q: 为什么不能用 "SA"?

**A**: 您的系统中已经安装了 Serena 工具,它占用了 "SA" 这个简称。为了避免混淆,我们使用完整名称 "SuperAgent" 或其他简称。

### Q: 我可以卸载 Serena 吗?

**A**: 如果您不使用 Serena 的代码搜索功能,可以禁用它。但建议保留,因为它也可能有用。

### Q: 最推荐的使用方式是什么?

**A**: 使用完整名称 "SuperAgent",这样最清晰明确:

```
使用 SuperAgent 开发...
```

---

## 📚 相关文档

- [GLOBAL_SETUP_GUIDE.md](e:\SuperAgent\GLOBAL_SETUP_GUIDE.md) - 全局配置指南
- [HOW_TO_USE_CORRECT.md](e:\SuperAgent\HOW_TO_USE_CORRECT.md) - 正确使用方式
- [README.md](e:\SuperAgent\README.md) - 项目总览
- [setup_superagent.py](e:\SuperAgent\setup_superagent.py) - 自动设置脚本

---

## 🎉 总结

### ✅ 使用要点

1. **不要使用 "SA"** (已被 Serena 占用)
2. **使用 "SuperAgent"** 完整名称 (推荐)
3. **或使用 "SAgent"** 简称
4. **或使用 "super"** 简称

### 💬 示例命令

```
✅ 使用 SuperAgent 开发用户登录功能
✅ 使用 SAgent 规划博客系统
✅ 使用 super 审查代码

❌ 使用 SA 开发...  (会启动 Serena!)
```

---

**开始使用**: "使用 SuperAgent 开发..."

**配置全局**: `python e:\SuperAgent\setup_superagent.py`

**祝使用愉快!** 🎉
