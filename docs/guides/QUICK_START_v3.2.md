# 🚀 SuperAgent v3.2 快速入门指南

> **在 Claude Code 中使用 SuperAgent v3.2 插件**

---

## 🎯 核心理念

**SuperAgent 是 Claude Code 的增强插件**

SuperAgent 不是一个独立的工具，而是作为 **Claude Code 的插件**，为 Claude Code 提供：
- ✅ 智能任务编排
- ✅ 多 Agent 管理
- ✅ 3层记忆系统
- ✅ 任务持久化
- ✅ 自动 Git 提交

---

## 📖 使用方式

### 步骤1: 设置环境变量

#### Linux/macOS

```bash
# 临时设置 (当前会话)
export SUPERAGENT_ROOT=/path/to/SuperAgent

# 永久设置 (添加到 ~/.bashrc 或 ~/.zshrc)
echo 'export SUPERAGENT_ROOT=/path/to/SuperAgent' >> ~/.bashrc
source ~/.bashrc
```

#### Windows (PowerShell)

```powershell
# 临时设置 (当前会话)
$env:SUPERAGENT_ROOT = "E:\SuperAgent"

# 永久设置
[System.Environment]::SetEnvironmentVariable('SUPERAGENT_ROOT', 'E:\SuperAgent', 'User')
```

### 步骤2: 启动 Claude Code

```bash
# 进入您的项目目录
cd /path/to/your/project

# 启动 Claude Code
claude-code
```

### 步骤3: 开始对话

在 Claude Code 中直接用自然语言描述需求：

```
你: 创建一个博客系统，包含文章管理和用户评论

Claude Code (使用 SuperAgent):
✅ SuperAgent 已生成任务计划
📋 任务 1/8: 创建项目结构...
📋 任务 2/8: 创建文章模型...
...
✅ 完成: 8/8 任务成功
```

---

## 💡 v3.2 新特性

### 1. 任务持久化和断点续传

**之前的问题**:
- ❌ Claude Code 关闭后，任务进度丢失
- ❌ 无法中断后继续

**v3.2 的改进**:
- ✅ 任务状态保存到 `tasks.json`
- ✅ 可随时中断和恢复
- ✅ 进度可视化

```
你: 继续开发博客系统

SuperAgent: 📋 从上次位置继续...
   已完成: 5/8 任务
   下一个: 添加评论功能
```

### 2. 自动 Git 提交

**v3.2 新功能**:
- ✅ 每个任务完成后自动 Git 提交
- ✅ 清晰的提交历史
- ✅ 描述性的 commit message

```
✅ 任务完成: 创建用户模型
📝 Git 提交: [Task-001] 实现用户模型
   + models/user.py (新增)
   + tests/test_user.py (新增)
```

### 3. 单任务焦点模式

**v3.2 新功能**:
- ✅ 防止上下文过大
- ✅ 自动拆分大任务
- ✅ 任务范围验证

```
SuperAgent: ⚠️ 任务过大，自动拆分为3个子任务
   📋 任务 2.1: 创建基础模型
   📋 任务 2.2: 添加验证逻辑
   📋 任务 2.3: 添加关联关系
```

---

## 🎮 实际使用示例

### 示例1: 创建 Web 应用

```
你: 创建一个 Flask 博客系统

Claude Code + SuperAgent:
✅ 理解需求
✅ 生成 8 个任务
📋 执行任务 1/8: 创建项目结构...
📋 执行任务 2/8: 创建文章模型...
...
✅ 所有任务完成
💾 自动 Git 提交: 8 个提交
```

### 示例2: 增量开发

```
你: 为博客系统添加用户登录

Claude Code + SuperAgent:
✅ 加载已有代码
✅ 分析项目结构
✅ 生成 3 个新任务
📋 执行任务 1/3: 添加用户模型...
📋 执行任务 2/3: 实现登录视图...
📋 执行任务 3/3: 添加认证中间件...
✅ 完成
💾 自动 Git 提交
```

### 示例3: 断点续传

```
你: 创建电商系统
(中断... Ctrl+C)

你: 继续开发电商系统

Claude Code + SuperAgent:
✅ 从 tasks.json 加载进度
📊 已完成: 5/20 任务
📋 继续执行任务 6/20...
```

---

## 🔧 配置选项

### 查看 SuperAgent 配置

SuperAgent 的配置文件: `config/settings.py`

```python
class SuperAgentConfig(BaseModel):
    """SuperAgent 全局配置"""

    # v3.2 新特性
    enable_task_persistence: bool = True      # 任务持久化
    enable_git_auto_commit: bool = True       # 自动 Git 提交
    enable_single_task_mode: bool = True       # 单任务模式
    max_files_per_task: int = 5               # 每个任务最多文件数
```

### 自定义配置

编辑 `config/settings.py`:

```python
# 禁用自动 Git 提交
enable_git_auto_commit: bool = False

# 调整任务文件限制
max_files_per_task: int = 10

# 禁用单任务模式
enable_single_task_mode: bool = False
```

---

## 📊 执行模式

SuperAgent 会自动选择执行模式：

### 自动模式 (默认)

```
任务数量 < 10:
  → 批量执行模式 (快速完成)

任务数量 ≥ 10:
  → 增量执行模式 (支持断点续传)
```

### 手动控制

```
你: 使用增量模式创建电商系统

Claude Code + SuperAgent:
✅ 使用增量执行模式
✅ 创建 tasks.json
✅ 执行任务...
💾 保存进度
```

---

## 🛠️ 高级功能

### 1. 查看任务状态

```
你: 显示当前任务进度

SuperAgent:
📊 项目进度: 电商系统
   总任务: 20
   已完成: 8
   进行中: 1
   待处理: 11
   进度: 40% [=======>....]
```

### 2. 恢复中断的任务

```
你: 恢复电商系统开发

SuperAgent:
✅ 从 tasks.json 加载
📋 下一个任务: 添加购物车功能
🚀 继续执行...
```

### 3. 查看 Git 历史

```
你: 显示最近的 Git 提交

SuperAgent:
📝 最近的提交:
  [Task-008] 添加商品搜索 (2分钟前)
  [Task-007] 实现购物车 (5分钟前)
  [Task-006] 添加用户认证 (10分钟前)
```

---

## 💻 Python API (开发者)

如果您需要在代码中直接使用 SuperAgent：

### ConversationManager API

```python
from conversation.manager import ConversationManager
from pathlib import Path

async def main():
    # 创建对话管理器
    manager = ConversationManager(project_root=Path("."))

    # 处理自然语言请求
    result = await manager.process("创建博客系统")

    # 查看结果
    print(result.summary)

if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
```

### Orchestrator API

```python
from planning.planner import ProjectPlanner
from orchestration.orchestrator import Orchestrator
from pathlib import Path

async def main():
    # 1. 规划
    planner = ProjectPlanner()
    plan = await planner.create_plan("创建博客系统")

    # 2. 执行
    orchestrator = Orchestrator(Path("."))
    result = await orchestrator.execute_plan(plan)

    print(f"完成: {result.completed_tasks}/{result.total_tasks}")

if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
```

---

## 🎯 使用技巧

### 1. 详细描述需求

```
❌ 你: 创建博客

✅ 你: 创建一个 Flask 博客系统，使用 SQLAlchemy，
        包含文章的增删改查、Markdown 编辑器、
        用户评论、标签分类功能
```

### 2. 分阶段开发

```
✅ 第1阶段: 创建核心功能
    你: 创建博客系统的基础功能

✅ 第2阶段: 添加用户功能
    你: 为博客添加用户登录和注册

✅ 第3阶段: 添加高级功能
    你: 添加搜索、推荐、数据分析
```

### 3. 利用断点续传

```
✅ 开始大型项目
    你: 创建电商系统

✅ 中断 (Ctrl+C)

✅ 恢复继续
    你: 继续开发电商系统
    ✅ 自动从上次位置继续
```

---

## 📚 相关文档

- 📖 [完整使用指南](COMPLETE_USER_GUIDE_v3.2.md) - 所有功能详解
- 🎯 [模式选择指南](MODE_SELECTION_GUIDE.md) - 选择执行模式
- 📋 [快速参考卡](../../QUICK_REFERENCE.md) - 命令速查
- 💻 [VS Code 使用提示](../../.vscode/SUPERAGENT_GUIDE.md) - IDE 集成

---

## 🎉 总结

### 核心要点

1. ✅ **Claude Code 插件** - SuperAgent 是插件，不是独立工具
2. ✅ **环境变量配置** - 设置 `SUPERAGENT_ROOT`
3. ✅ **自然语言交互** - 在 Claude Code 中对话
4. ✅ **v3.2 新特性** - 任务持久化、自动 Git 提交、单任务模式

### 快速开始

```bash
# 1. 设置环境变量
export SUPERAGENT_ROOT=/path/to/SuperAgent

# 2. 进入项目目录
cd /path/to/your/project

# 3. 启动 Claude Code
claude-code

# 4. 开始对话
# (直接用自然语言描述需求)
```

---

**版本**: v3.2.0
**更新**: 2026-01-14
**维护**: SuperAgent Team

**🚀 在 Claude Code 中体验 SuperAgent v3.2 的强大功能！**
