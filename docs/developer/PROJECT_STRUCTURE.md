# SuperAgent v3.4 项目结构

> **生成时间**: 2026-01-28
> **版本**: v3.4.0

---

## 📁 目录结构

```
SuperAgent/
├── .superagent/                # 运行时持久化数据 (由 TaskListManager 管理)
│   ├── memory/                 # 记忆持久化
│   └── tasks.json              # 任务进度持久化
│
├── adapters/                    # 适配器层
│   ├── executor_adapter.py     # 执行器适配器
│   ├── reviewer_adapter.py      # 审查器适配器
│   └── unified_adapter.py       # 统一适配器 (v3.4 核心)
│
├── common/                      # 公共模块
│   ├── models.py               # 公共数据模型 (AgentType, TaskStatus)
│   └── ...
│
├── core/                        # 核心层 (抽象接口与基础工具)
│   ├── task_list_manager.py    # 任务列表管理器
│   ├── executor.py             # 执行器 ABC
│   └── reviewer.py             # 审查器 ABC
│
├── docs/                        # 文档系统 (已重构)
│   ├── user/                   # 用户指南
│   ├── developer/              # 开发者文档
│   ├── reports/                # 任务报告与总结
│   └── archive/                # 历史存档
│
├── execution/                   # 执行层 (Agent 具体实现)
│   ├── base_agent.py           # Agent 基类
│   ├── coding_agent.py         # 代码 Agent
│   └── ...
│
├── extensions/                  # 扩展层 (模块化重构)
│   ├── executors/              # 自定义执行器 (Writing, N8n, Prompt)
│   ├── reviewers/              # 自定义审查器 (Content)
│   ├── hooks/                  # 生命周期钩子系统
│   ├── skills/                 # 动态技能系统
│   └── state_persistence/      # 会话状态持久化 (SessionManager)
│
├── memory/                      # 记忆系统 (3层架构)
│
├── orchestration/               # 编排层 (调度与工厂)
│   ├── orchestrator.py         # 核心编排器
│   ├── agent_factory.py        # Agent 工厂
│   └── registry.py             # Agent 注册表
│
├── server/                      # 服务层 (v3.4 新增)
│   ├── interaction_service/    # NLP, Dispatcher, ProjectGuide
│   ├── fastapi_app.py          # REST API 服务
│   └── mcp_server.py           # MCP 协议服务
│
└── tests/                       # 测试套件 (Unit, Integration, E2E)
```

---

## 🎯 v3.4 核心架构变更

### 1. 扩展模块化 (Extensions Modularization)
为了解决 `extensions/` 目录文件过多的问题，现已将其按功能拆分为 `executors`、`reviewers`、`hooks` 和 `state_persistence` 子目录。自定义组件应注册在 `orchestration/registry.py` 中。

### 2. 统一持久化 (.superagent/)
所有运行时生成的 JSON 和日志文件现统一存储在根目录下的 `.superagent/` 文件夹中，避免污染项目根目录。

### 3. 服务化转型 (Server & NLP)
新增 `server/` 目录，支持通过 FastAPI 提供 REST 接口，并引入了自然语言解析器（NLP）和项目引导系统（ProjectGuide）。

---

## 📖 文档导航

- [主文档索引](../INDEX.md)
- [快速入门指南](../user/QUICK_START_v3.2.md)
- [开发者 API 参考](AGENT_API_REFERENCE.md)

**版本**: v3.4.0
**最后更新**: 2026-01-28
